# Release Artifacts

Place generated, signed AABs for distribution in this directory. For example:

```bash
cp app/build/outputs/bundle/release/app-release.aab release/DreamZ-v1.0.aab
```

The `.gitignore` is configured to track files inside `release/`, so clean up any obsolete bundles before committing.
